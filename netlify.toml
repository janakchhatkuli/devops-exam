[build]
  command = "echo 'Building frontend'"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "http://localhost:8080/:splat"
  status = 200

[dev]
  command = "echo 'Frontend ready on port 3000'"
  port = 3000
